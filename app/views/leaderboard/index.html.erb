<h1>🏆 Kudo Leaderboard (Last 30 Days)</h1>

<table>
  <thead>
    <tr>
      <th>Rank</th>
      <th>User</th>
      <th>Kudos Received</th>
    </tr>
  </thead>

  <tbody>
    <% @leaders.each_with_index do |user, index| %>
      <tr>
        <td><%= index + 1 %></td>
        <td><%= user.username %> (<%= user.email %>)</td>
        <td><%= user.kudos_count %></td>
      </tr>
    <% end %>
  </tbody>
</table>
<p>
  <%= link_to "← Back to Dashboard", dashboard_path, class: "btn btn-link" %>
</p>